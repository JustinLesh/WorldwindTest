import gov.nasa.worldwindx.examples.*;

import javax.swing.*;

public class test {
    public static void main(String[] args) {
        //ApplicationTemplate.start("WorldWind Bulk Download", BulkDownload.AppFrame.class);
        //ApplicationTemplate.start("Shape Editing", ShapeEditing.AppFrame.class);
        ApplicationTemplate.start("Ellipsoids", Ellipsoids.AppFrame.class);
    }
}
